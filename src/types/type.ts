export type ReactionType = "like" | "wow" | "sad" | "haha" | "heart" | "angry";

export type UserType = {
  username: string;
  email: string;
  profile: string;
  _id: string;
  createdAt: string;
  updatedAt: string;
};
